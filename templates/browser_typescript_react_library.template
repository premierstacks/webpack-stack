import { browserTypescriptReactLibrary } from '@premierstacks/webpack-stack';

export default function (env, argv) {
  const config = browserTypescriptReactLibrary(env, argv);

  config.entry = { index: ['./src/index.ts'] };

  return config;
}
